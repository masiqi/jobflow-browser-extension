# 统一语言

> 状态：首期需求已确认；“已标记歧义”中的未来能力不属于当前实现依据。

## 参与者与外部系统

| 术语 | 定义 | 避免使用的别名 |
| --- | --- | --- |
| **求职者** | 使用本产品发现和联系招聘机会的人。 | 候选人、用户（指业务角色时） |
| **产品账号** | 标识一个已认证产品用户并界定其云端数据所有权的身份。 | 求职者（指身份时）、招聘平台账号 |
| **邮箱认证状态** | 由产品后端维护、表示产品账号是否完成邮箱所有权验证的状态。 | Supabase 登录状态、邮箱字段 |
| **招聘方联系人** | 在招聘平台上代表用人方与求职者沟通的人。 | HR、猎头、招聘者（未区分身份时） |
| **招聘平台** | 承载职位搜索、职位详情和招聘沟通的第三方网站。 | 网站、渠道 |
| **模型服务** | 接收受控上下文并返回结构化评估或沟通草稿的 LLM 服务。 | AI、模型（指外部系统时） |
| **托管模型权益** | 允许符合会员条件的产品账号使用产品方模型凭证和受限额度的权限。 | VIP（指整个账号状态时）、免费模型 |
| **自带模型凭证** | 求职者提供自己的模型服务凭证并承担该服务用量的处理方式。 | 自定义模型、BYOK（未展开时） |
| **模型路由** | 一次模型处理明确选择的托管模型权益或自带模型凭证路径。 | 自动降级、模型配置 |
| **产品后端** | 为产品账号提供认证后数据、文件和模型调用能力的托管服务。 | 模型服务、招聘平台 |
| **用户数据域** | 由一个产品账号独占且默认不能被其他账号访问的数据集合。 | 租户、账号（指数据时） |

## 求职资料

| 术语 | 定义 | 避免使用的别名 |
| --- | --- | --- |
| **简历原文** | 求职者导入、包含其完整经历陈述的源文档文本。 | 画像、简历事实 |
| **本地简历副本** | 仅保存在当前客户端、供处理或未来投递使用且不会同步到产品后端的原始简历文件。 | 云端简历、简历画像 |
| **规范化简历文本** | 客户端从受支持来源提取并标准化、仅在画像处理期间使用的临时文本。 | 原始文件、简历画像 |
| **简历画像** | 从一个确定版本的简历原文提取并经求职者确认的结构化事实集合。 | 简历、用户画像 |
| **待审核画像** | 已从简历原文提取但尚未由求职者确认、不得用于职位处理的画像修订。 | 草稿简历、当前画像 |
| **已启用画像** | 已由求职者明确确认且允许用于职位处理的当前画像修订。 | 已解析简历、默认画像 |
| **已过期画像** | 因权威本地简历发生变化而不再允许用于职位处理的旧画像修订。 | 历史简历、已启用画像 |
| **简历事实** | 可追溯到简历原文证据且允许用于评估和生成草稿的单项陈述。 | 卖点、能力标签 |
| **平台简历绑定** | 求职者明确确认某份招聘平台简历与权威本地简历版本相对应的关系。 | 默认简历、自动匹配 |
| **求职偏好** | 求职者对目标职位的期望条件和取舍。 | 筛选规则、画像 |

## 职位发现与判断

| 术语 | 定义 | 避免使用的别名 |
| --- | --- | --- |
| **职位线索** | 从招聘平台列表页发现但尚未完成详情核验的职位记录。 | 候选人、候选职位 |
| **职位详情** | 从招聘平台详情页读取的、用于最终判断的职位信息。 | JD（泛指整条记录时） |
| **职位描述** | 职位详情中描述职责和要求的文本。 | 职位详情 |
| **平台职位标识** | 招聘平台为一条职位记录提供并由产品原样使用的唯一标识。 | 公司职位名称、职位指纹 |
| **平台粗筛** | 求职者使用招聘平台原生搜索与筛选控件缩小职位结果集的步骤。 | 自动筛选、JD 筛选 |
| **结果快照** | 用户触发扫描时招聘平台当前 DOM 中已经加载的职位卡片集合。 | 搜索结果（未说明时间边界时）、整站结果 |
| **连续采集** | 通过受控翻页或滚动把多个结果快照并入同一发现流程的后续能力。 | 当前扫描、爬取整站 |
| **JD 细筛** | 产品依据完整职位描述和封闭规则目录判断职位是否继续处理的步骤。 | 平台筛选、自由文本规则 |
| **JD 规则目录** | 产品定义并版本化、供求职者通过结构化控件启停的有限规则集合。 | 自定义关键词、自然语言要求 |
| **规则证据** | 支持某条 JD 规则判断的最小职位描述原文片段。 | 模型理由、匹配词 |
| **硬性排除条件** | 一旦命中就必须淘汰职位且不得由模型推翻的确定性条件。 | 偏好、负向关键词 |
| **软性偏好** | 影响职位排序或适配度、但不单独决定淘汰的条件。 | 硬规则、加分项 |
| **人工复核** | 因信息不足、冲突或低置信度而由求职者作出最终决定的步骤。 | 失败、跳过 |
| **复核项** | 记录规则或模型的复核来源、理由和证据并等待求职者决定的职位机会状态。 | 失败项、模型淘汰 |
| **模型评估** | 模型基于特定 JD、已启用画像和版本化策略产生的结构化职位判断。 | JD 规则判断、文案生成 |
| **模型淘汰** | 一个经校验并持久记录、永久阻止同一平台职位标识自动处理的模型评估结论。 | 硬规则排除、临时跳过 |
| **用户排除** | 求职者在复核中作出并永久阻止同一平台职位标识自动处理的决定。 | 模型淘汰、临时取消 |
| **用户例外** | 求职者保留原排除证据并明确要求某个被排除职位进入生成步骤的覆盖决定。 | 重新评估、删除排除 |
| **职位机会** | 以招聘平台和平台职位标识唯一确定、并具有持续跟踪生命周期的对象。 | 职位线索、投递记录 |

## 联系与跟踪

| 术语 | 定义 | 避免使用的别名 |
| --- | --- | --- |
| **招呼语草稿** | 基于职位详情和已确认简历事实生成、尚未发送的首次联系文本。 | 招呼语（未说明是否发送）、消息 |
| **草稿修订** | 一次不可覆盖的模型生成或用户编辑版本及其生成上下文。 | 当前草稿、发送历史 |
| **待发消息** | 由求职者审阅并可编辑、但尚未对招聘平台发送的消息版本。 | 招呼语草稿、已发送消息 |
| **待发列表** | 集中展示待发消息并支持单条操作和多选操作的工作队列。 | 台账、发送历史 |
| **首次联系** | 求职者首次向某个职位机会的招聘方联系人发送消息的已验证事件。 | 生成招呼语、投递 |
| **正式投递** | 求职者明确申请一个职位并向招聘平台提交其简历的已验证事件。 | 首次联系、生成草稿 |
| **投递并联系** | 针对同一职位机会正式投递简历并发送招呼语的复合业务意图。 | 发送、投递（未说明组成动作时） |
| **部分完成** | 投递并联系的两个组成动作中只有一个取得平台回读证据的结果。 | 成功、失败 |
| **招聘会话** | 围绕一个职位机会发生的一组有序站内消息。 | 投递、跟进 |
| **会话消息** | 招聘会话中由求职者或招聘方联系人发送的一条消息。 | 回复、内容（未说明方向时） |
| **明确拒绝** | 招聘方联系人以足够明确的语义表示不再推进该职位机会的终止结果。 | 不合适、已读不回 |
| **站外联系方式交换** | 双方达成可在邮箱、电话、微信等站外渠道继续联系的可验证结果。 | 联系方式出现、联系方式提取 |
| **跟踪中** | 已首次联系且尚未达到终止结果的职位机会状态。 | 已投递、进行中 |
| **终止结果** | 使产品停止自动跟踪职位机会的明确拒绝或站外联系方式交换。 | 完成、关闭 |

## 运行与证据

| 术语 | 定义 | 避免使用的别名 |
| --- | --- | --- |
| **处理批次** | 求职者一次启动、包含有限数量职位线索的处理集合。 | 任务、投递批次 |
| **平台每日额度** | 一个用户在一个招聘平台自然日内允许开始的投递并联系机会数量。 | 平台官方上限、批次上限 |
| **页面入口** | 显示在支持的招聘平台页面边缘、用于打开扩展侧边栏的轻量控件。 | 浮动面板、管理台 |
| **侧边栏工作台** | 求职者浏览招聘平台时进行当前页面扫描和批次操作的扩展界面。 | 页面入口、管理台 |
| **扩展管理台** | 用于画像、规则、模型、完整待发列表和台账等密集管理工作的全页扩展界面。 | 侧边栏、网页浮层 |
| **模拟运行** | 读取和评估职位并生成草稿、但不对招聘平台执行任何写操作的处理批次。 | 模拟投递、成功投递 |
| **执行策略** | 决定系统只生成草稿、经人工确认发送或允许自动发送的产品级行为边界。 | 自动发送开关、运行模式 |
| **仅生成** | 只创建待发消息且禁止任何招聘平台写操作的执行策略。 | 关闭自动发送、模拟投递 |
| **审核后发送** | 求职者审阅待发消息并明确确认单条或所选批次后才允许发送的执行策略。 | 手动发送、半自动发送 |
| **自动发送** | 求职者对当前 side panel 勾选批次做一次明确授权后，系统对本批中模型判定可继续且草稿有效的职位自动执行投递并联系；不需要逐条确认，也不处理历史草稿或用户例外草稿。 | 批量发送、审核后发送、无需授权发送 |
| **本地台账** | 在当前扩展存储中记录职位机会状态、证据和时间的持久化数据。 | 历史记录、投递记录 |
| **平台回读证据** | 从招聘平台界面重新读取并能证明某个外部动作实际发生的证据。 | API 成功、点击成功 |

## 关系

- 一份 **简历原文** 生成一个有版本的 **简历画像**，一个 **简历画像** 包含多个 **简历事实**。
- 一个 **本地简历副本** 通过内容哈希关联一个简历画像，但文件本身不得持久化到产品后端。
- 本地简历副本或粘贴文本产生规范化简历文本，模型只能从该临时文本抽取待审核画像。
- 待审核画像经求职者明确确认后成为已启用画像；更换权威本地简历会使原已启用画像成为已过期画像。
- 只有已启用画像中的简历事实可以参与职位判断和消息生成。
- 本地简历副本是权威版本；无法直接提交该文件时，正式投递必须使用已建立平台简历绑定的招聘平台简历。
- 本地简历副本缺失、内容哈希不匹配或平台简历绑定不明确时，不得执行正式投递。
- 一个 **产品账号** 对应一个用户数据域，简历、偏好、职位机会、待发消息和台账都必须归属于该数据域。
- 邮箱认证状态不影响首期核心求职流程，也不能由产品账号自行修改。
- 一次模型处理只使用一个明确的模型路由，失败时不得静默切换托管模型权益、自带模型凭证、供应商或模型。
- 一个 **职位线索** 经详情核验后对应一个 **职位机会**；在一个 **用户数据域** 内，相同 **招聘平台** 与 **平台职位标识** 不得产生多个职位机会。
- 不同 **平台职位标识** 始终代表不同 **职位机会**，即使其公司、标题或职位描述相同也不得合并或标记疑似重复。
- **模型淘汰** 只按用户数据域、招聘平台和平台职位标识复用，职位内容及所有评估输入变化都不会使其失效。
- 用户例外不会使原排除失效或触发重新评估，只会允许该职位进入受事实校验的生成步骤。
- 平台粗筛产生职位线索；JD 细筛依据完整职位描述、JD 规则目录和规则证据决定继续、排除或人工复核。
- 通过 JD 细筛的职位机会可以接受模型评估；有效的模型淘汰会阻止草稿生成并在后续扫描中复用。
- 规则歧义或模型复核产生复核项；求职者可以继续生成或形成用户排除，未决定的复核项不得重复处理。
- 首期一次扫描只读取一个结果快照；未来连续采集产生多个结果快照，但共享同一职位机会去重规则。
- 一个 **职位机会** 至多有一个活跃的 **招聘会话**，一个招聘会话包含多条有序的 **会话消息**。
- 一次 **首次联系** 必须属于一个职位机会，并以 **平台回读证据** 证明已经发送。
- 一次 **投递并联系** 包含一次正式投递和一次首次联系，两个组成动作必须分别记录平台回读证据。
- 只有正式投递和首次联系均成功时，投递并联系才成功；任一动作未成功都必须保留可恢复的部分完成状态。
- **投递并联系** 优先采用可分别验证结果的平台原生组合动作；必须分步时先正式投递、后首次联系。
- 部分完成后的重试只执行尚无成功证据的组成动作，不得重复已验证的正式投递或首次联系。
- 一个 **职位机会** 在 **明确拒绝** 或 **站外联系方式交换** 后达到终止结果。
- 一个 **处理批次** 可以处理多个职位线索，但不能把 **模拟运行** 的结果记为首次联系。
- 处理批次上限控制一次运行规模，平台每日额度控制未来真实投递并联系规模，两者不得混用。
- 平台每日额度按北京时间自然日统计；一次投递并联系只要开始任一真实平台写操作就占用一个单位，同一职位的部分完成重试不重复占用。
- 一个 **招呼语草稿** 可以产生一个可编辑的 **待发消息**，但编辑和保存都不构成首次联系。
- 一个待发消息只有一个当前文本，但可以包含多个按时间保留且不可覆盖的草稿修订。
- **仅生成** 是首个可验收版本的强制执行策略；审核后发送已完成单职位人工验收，自动发送已进入选中批次实现与合成验证阶段，但仍需真实 Chrome/猎聘人工验收后才能描述为现场验收通过。

## 示例对话

> **开发者：** “模型生成了招呼语后，这个职位机会算已首次联系或正式投递吗？”
>
> **领域专家：** “都不算，那只是招呼语草稿；未来的投递并联系还需要分别取得正式投递和首次联系的回读证据。”
>
> **开发者：** “招聘方联系人回复‘暂时不考虑’，还要继续跟踪招聘会话吗？”
>
> **领域专家：** “若它符合我们对明确拒绝的判定标准，就记录终止结果；低置信度表达应进入人工复核。”
>
> **开发者：** “消息中出现一个手机号就算站外联系方式交换吗？”
>
> **领域专家：** “不一定；联系方式出现和双方达成交换是不同事件，判定标准仍待产品确认。”

## 已标记歧义

- “发送”在待发列表上下文中已确定为 **投递并联系**，不是仅发送招呼语；描述结果时仍必须分别使用正式投递与首次联系。
- “打过招呼”可能指生成了草稿，也可能指消息确已发送；只有具备平台回读证据的后者才是 **首次联系**。
- “上传简历”应按目的明确表述：**导入本地简历副本**、临时发送给模型处理或向招聘平台正式投递；产品后端不得持久化原文件。
- “画像生成完成”不代表画像已启用；模型抽取、本地回退抽取和用户确认是三个不同事件。
- “明确拒绝”尚未定义判定主体、置信度阈值、否定表达和人工复核规则。
- “交换站外联系方式”尚未明确是联系方式被提及、被一方发送、被双方确认，还是已在站外建立联系。
- “继续沟通”尚未明确自动化程度、每次发送前是否人工确认、跟进频率、静默超时和最大轮次。
- “JD”有时被用于指职位描述文本，有时被用于指整条职位记录；前者统一称为 **职位描述**，后者统一称为 **职位机会**，其身份只由招聘平台和平台职位标识决定。
